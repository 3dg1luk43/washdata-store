# Internal planning / dev notes - not for the public repo
washdata-store-plan.md
SETUP.md
__pycache__/
node_modules/

# Firebase CLI local state
.firebase/
firebase-debug.log
firestore-debug.log

# OS / editor cruft
.DS_Store
*.swp

# Built by scripts/build_search_index.mjs and published straight from the deploy workflow.
# Deliberately NOT committed: it is ~300 KB that changes whenever the catalog does, and the
# deploy rebuilds it anyway. `npm run build:search-index` makes one for local testing.
search-index.json
